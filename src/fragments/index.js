import HeaderMenuFragment from './header';

const {
  vendor: {
    react: React,
  },
} = platform;

export default {
  header: [{
    comp: <HeaderMenuFragment key="support-chain-header" />,
  }],
};
